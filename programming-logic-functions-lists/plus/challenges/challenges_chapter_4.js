// Create an empty list, named genericList.
let genericList = []; // An empty array is initialized

// Create a list of programming languages ​​called `programmingLanguages` with the following elements: 'JavaScript', 'C', 'C++', 'Kotlin' and 'Python'.
let programmingLanguages = ['JavaScript', 'C', 'C++', 'Kotlin', 'Python']; // Initializing array with predefined elements

// Add the following elements to the Programming Languages ​​list: 'Java', 'Ruby' and 'GoLang'.
programmingLanguages.push('Java', 'Ruby', 'GoLang'); // Adding multiple elements to the list using push

// Create a list with 3 names and display only the first element in the console.
let names = ['John', 'Doe', 'Jane']; // Initializing an array with names
console.log(names[0]); // Accessing and displaying the first element

// Create a list with 3 names and display only the second element in the console.
console.log(names[1]); // Accessing and displaying the second element

// Create a list with 3 names and display only the last element in the console.
console.log(names[names.length - 1]); // Dynamically accessing and displaying the last element
