// 1 - Show an alert with the message "Welcome to our website!".
alert("Welcome to our website!");

// 2 - Declare a variable called `name` and assign it the value "Lua".
let name = "Lua";

// 3 - Create a variable called `age` and assign it the value 25.
let age = 25;

// 4 - Define a variable `numOfSales` and assign it the value 50.
let numOfSales = 50;

// 5 - Define a variable `availableBalance` and assign it the value 10000.
let availableBalance = 10000;

// 6 - Display an alert with the text "Error! Please fill in all fields."
alert("Error! Please fill in all fields.");

// 7 - Declare a variable called `errorMessage` and assign it the value "Error! Please fill in all fields.". Now display an alert with the value of the ErrorMessage variable.
let errorMessage = "Error! Please fill in all fields.";
alert(errorMessage);

// 8 - Use a prompt to ask for the user's name and store it in the `userName` variable.
let userName = prompt("What's your name?");
console.log(userName);

// 9 - Ask the user to enter their age using a prompt and store it in the `userAge` variable.
let userAge = prompt("How old are you?");
console.log(age);

// 10 - Now, if the age is greater than or equal to 18, display an alert with the message "You can get your license!".
if (userAge >= 18) {
  alert("You can get your license!");
}